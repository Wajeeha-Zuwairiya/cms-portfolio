{
  "name": "cms-portfolio-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "vite": "^5.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
